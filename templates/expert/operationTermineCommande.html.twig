{% extends 'base.html.twig' %}

{% block title %}Dashboard
{% endblock %}

{% block navbarlists %}
	<lu>
		<a href="{{ path('app_expert') }}">Tableau de bord</a>
		<br/><br/>
		<a href="{{ path('app_expert_operations') }}">Ajouter une opération</a>
		<br/><br/>
		<a href="{{ path('app_liste_operations') }}">Lister mes opérations</a>
		<br/><br/>
		<a href="{{ path('liste_employe') }}">Liste des employés</a>
		<br/><br/>
		<a href="{{ path('inscription') }}">Ajouter un employé</a>
		<br/><br/>
		<a href="{{ path('liste_employe_modifier') }}">Modifier un employé</a>
		<br/><br/>
		<a href="{{ path('app_prise_commande_expert') }}">Ajouter commande client</a>
		<br/><br/>
		<a href="{{ path('app_home') }}">Chiffre d'affaire</a>
	</lu>

{% endblock %}

{% block main2 %}

	<h2>Terminer la commande</h2>
	{{ form_start(form) }}
	{{ form_row(form.nomCommande, {"attr": {"class": "btn disabled"}})}}
	{{ form_row(form.date, {"attr": {"class": "btn disabled"}})}}
	{{ form_row(form.statut, {"attr": {"class": "btn disabled"}})}}
	{{ form_row(form.client, {"attr": {"class": "btn disabled"}})}}
	{{ form_row(form.operation,{"attr": {"class": "btn disabled"}})}}
	{{ form_row(form.user,{"attr": {"class": "btn disabled"}})}}
	<button type="submit" class="btn btn-primary">Comfirmer</button>
	{{ form_end(form) }}

{% endblock %}
